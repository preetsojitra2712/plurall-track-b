# Core — install these first, they cover 90% of what you need
numpy>=1.26
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3
pillow>=10.0
matplotlib>=3.7

# Vision / models — heavier, install once and cache the weights
torch>=2.2
torchvision>=0.17
timm>=1.0
transformers>=4.40
peft>=0.10
open_clip_torch>=2.24
opencv-python-headless>=4.9
facenet-pytorch>=2.5      # MTCNN face detection + alignment

# Audio / data sourcing
datasets>=2.19          # streaming pulls, so no full-archive download
soundfile>=0.12         # libsndfile bindings, wav read/write

# Speech synthesis — generated positives. Two architecturally distinct engines
# on purpose: one generator only ever teaches you its own fingerprint.
kokoro>=0.9             # Apache-2.0, 82M StyleTTS2-class, CPU-fine
piper-tts>=1.6          # VITS/onnxruntime, CPU real-time

# Experiment tracking / utils
tqdm>=4.66
pyyaml>=6.0
